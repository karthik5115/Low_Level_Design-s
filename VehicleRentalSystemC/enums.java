enum Type {
    Bike,
    Car;
}

enum Status {
    active,
    inactive;
}

enum ReservationStatus {
    Completed,
    Pending,
    Cancelled;
}

enum PaymentStatus {
    Paid,
    Pending,
    Failed;
}